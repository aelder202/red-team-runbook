!!! tip "Tip"
    Pick the right tool for the situation: Ligolo-ng for full subnet pivoting, Chisel for a quick single-port reverse tunnel, SSH `-L`/`-R` when SSH is already available, and `socat` for a fast one-shot relay without installing anything extra.
